<div class="app-content">
  {{!-- defaulting to alarm guest --}}
  <div class="full-width">
    <div id = 'alarm'>
      <div class = 'full-width'>
        <h1>no clock</h1>
        <p>
          Most alarm features are unavailable without javascript enabled, but you can add, update and delete alarms here.
        </p>
      </div>
    </div>
    {{!-- <div class='flex space-around'>
      <div class='flex center'>
        <h2 id='clock'></h2>
      </div>
    </div> --}}
    <div class="listWrapper">
      {{#each alarmContent}} {{> alarmdiv}} {{/each}}
    </div>
    <p class="formErrorReport">{{ dbError }}</p>
    {{!-- <form action='/accounts/{{email}}/transact' method='post'>
      <button class='yes'>finish month</button>
    </form> --}}
    <form action="/app/accounts/{{email}}/alarms/new-alarm" method="GET">
      <button class="button dark-button"> add alarm </button>
    </form>
    <form action='/app/accounts/{{email}}/alarms?_method=DELETE' method="POST">
      <button class="button light-button"> delete all alarms </button>
    </form>
  </div>
</div>
<script src='/rollup/bundle.js'></script>
<script>
  a.alarmClock()
</script>